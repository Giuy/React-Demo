type UserType = {
  userName: string;
  personalNumber: string;
  name: string;
  accessToken: string;
  refreshToken: string;
  isLockedOut: boolean;
  succeeded: boolean;
  userAvatar: string;
  customer: [];
};

export type { UserType };
